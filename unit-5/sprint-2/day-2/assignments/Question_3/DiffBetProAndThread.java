package Question_3;
/*
 * Process:-
 *     -> An application under execution is called a process.
 *     -> In process context switching happens slow.
 *     -> Each process allocates separate memory area.
 *     -> Process is heavy weight.
 *     -> Cost of communication between two processes is high.
 * 
 * Thread:-
 *     -> Thread is a sub-process or smallest unit of processing.
 *     -> In thread context switching happens faster.
 *     -> Threads share the same address.
 *     -> Threads are light weight.
 *     -> cost of communication between two thread is low
 */

public class DiffBetProAndThread {

}
